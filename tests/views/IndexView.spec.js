import React from 'react'
import { IndexView } from 'views/IndexView'
import { mountWithIntl } from '../helpers/intl'

describe('(View) Index', () => {
  let _component

  beforeEach(() => {
    _component = mountWithIntl(<IndexView />)
  })
})
